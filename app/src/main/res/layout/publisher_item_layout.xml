<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <de.hdodenhof.circleimageview.CircleImageView
        android:id="@+id/publisher_icon"
        android:layout_width="@dimen/publisher_icon"
        android:layout_height="@dimen/publisher_icon"
        android:layout_marginBottom="8dp"
        android:layout_marginStart="8dp"
        android:layout_marginTop="8dp"
        android:src="@mipmap/ic_launcher_round"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_bias="0.0" />

    <TextView
        android:id="@+id/publisher_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="8dp"
        android:layout_marginStart="16dp"
        android:layout_marginTop="8dp"
        android:text="Publisher Name"
        android:textSize="@dimen/publisher_name_text_size"
        app:layout_constraintBottom_toBottomOf="@+id/publisher_icon"
        app:layout_constraintStart_toEndOf="@+id/publisher_icon"
        app:layout_constraintTop_toTopOf="@+id/publisher_icon"
        app:layout_constraintVertical_bias="0.19999999" />

    <TextView
        android:id="@+id/publisher_timestamp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:text="Publisher Timestamp"
        android:textSize="@dimen/publisher_timestamp_text_size"
        app:layout_constraintEnd_toEndOf="@+id/publisher_name"
        app:layout_constraintHorizontal_bias="0.0"
        app:layout_constraintStart_toStartOf="@+id/publisher_name"
        app:layout_constraintTop_toBottomOf="@+id/publisher_name" />

</android.support.constraint.ConstraintLayout>